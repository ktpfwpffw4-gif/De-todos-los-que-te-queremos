// Copia este archivo a `firebase-config.js` y añade la URL de tu
// Realtime Database de Firebase. No subas `firebase-config.js` al repo.
window.CARTAS_DB_CONFIG = {
  // Ejemplo: https://mi-proyecto-default-rtdb.firebaseio.com
  firebaseDatabaseURL: ""
};
